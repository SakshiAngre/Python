"""Tuples are ordered but immutable collections (cannot be changed after creation)."""
tup=(10, 20, 30)

print(tup[0]) 

a=(4,)
"""to store single element in tuple, (,) is used because (paranthisis) is used for group together    """

#tuple unpacking

a=(3,2,4,5)

a1,b2,c3,d4=a

print(a1,b2,c3,d4)
